import SwiftUI

struct SliceApp: App {
    var body: some Scene {
        WindowGroup("Taskflow Native Slice") {
            Text("Native slice — scaffold")
                .frame(minWidth: 1100, minHeight: 720)
        }
    }
}
